{
  "name": "json-validator",
  "version": "1.0.0",
  "description": "CLI tool to validate JSON files against JSON schemas",
  "main": "dist/index.js",
  "bin": {
    "json-validator": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["json", "validator", "schema", "cli"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "debug": "^4.3.4",
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/debug": "^4.1.12",
    "typescript": "^5.3.0"
  }
}
